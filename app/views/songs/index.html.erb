<h1>List of Songs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
    </tr>
  </thead>
  <tbody>
    <% @songs.each do |song| %>
    <tr>
      <th><%= link_to song.title, song_path(song) %></th>
    </tr>
    <% end %>
  </tbody>
</table